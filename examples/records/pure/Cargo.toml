[package]
name = "record-pure"
version = "0.1.0"
authors = ["Fluence Labs"]
edition = "2018"
publish = false

[[bin]]
name = "records_pure"
path = "src/main.rs"

[dependencies]
fluence = { version = "=0.4.0", features = ["logger"]}
test-record = { path = "../test-record" }
