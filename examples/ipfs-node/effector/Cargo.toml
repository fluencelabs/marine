[package]
name = "ipfs-effector"
version = "0.1.0"
authors = ["Fluence DAO, Clouldless Labs"]
edition = "2021"
publish = false

[[bin]]
name = "ipfs_effector"
path = "src/main.rs"

[dependencies]
marine-rs-sdk = { version = "0.14.0", features = ["logger"] }
log = "0.4.14"
