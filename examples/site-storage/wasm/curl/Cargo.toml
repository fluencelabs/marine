[package]
name = "wasm_curl"
version = "0.1.0"
authors = ["Fluence Labs"]
edition = "2018"

[lib]
name = "wasm_curl"
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
fluence = { git = "https://github.com/fluencelabs/rust-sdk", features = ["logger"] }
log = "0.4.8"
